{
  "name": "ig_bot_test",
  "version": "1.0.0",
  "description": "",
  "main": "build/app.ts",
  "scripts": {
    "start": "node -r ./tsconfig-paths-prod.js ./build/app.js",
    "build": "rm -rf ./build && npx tsc && cp -R ./src/lib/types ./build/lib/types",
    "heroku-postbuild": "echo Skip build on Heroku",
    "dev": "npx tsnd --watch ./src -r tsconfig-paths/register --respawn --transpile-only ./src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "bootbot": "^1.1.0",
    "express": "^4.17.1",
    "tsconfig-paths": "^3.10.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.4.5",
    "dotenv": "^10.0.0",
    "ts-node": "^10.1.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"
  }
}
